using UnityEngine;
using System.Collections;

// This script must added to the game rocker. 
// And it’s used to pass the player’s controls to the GameController
public class GameRocker : MonoBehaviour {
	
	// Use this for initialization
	// Instantiate a controller 
	void Start () {
		//TODO assign the controller form maybe gameManeger
	}
	
	// Update is called once per frame
	void Update () {
		//TODO pass player’s control to the controller.It will have direction,power and the skill which user chose.
	}
}
